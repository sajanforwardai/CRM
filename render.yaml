services:
  # Streamlit CRM App
  - type: web
    name: forwardai-crm
    runtime: python-3
    plan: starter
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py
    envVars:
      - key: DATABASE_URL
        sync: false
